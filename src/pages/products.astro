---

import { getEntry, render } from "astro:content";
import Markdown from "@components/common/Markdown.astro";
import MainGridLayout from "@/layouts/MainGridLayout.astro";

const productPost = await getEntry("spec", "products");

if (!productPost) {
	throw new Error("Products page content not found");
}

const { Content } = await render(productPost);
---
<MainGridLayout title="资料库" description="整理 AI 内容运营系统里的模板、清单和本地工作流，发布前先审查。">
    <div class="flex w-full rounded-(--radius-large) overflow-hidden relative min-h-32">
        <div class="card-base z-10 px-9 py-6 relative w-full ">
            <Markdown class="mt-2">
                <Content />
            </Markdown>
        </div>
    </div>
</MainGridLayout>
